<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moses.tuan">


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".PagerActivity" />

        <activity android:name=".LogInActivity" />

        <activity android:name="FragmentTabHostActivity" />

        <activity android:name="com.moses.tuan.database.DataBaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.moses.tuan.sign.MainSignActivity">


        </activity>
        <activity android:name="com.moses.tuan.sign.SignUpActivity" />

    </application>

</manifest>
